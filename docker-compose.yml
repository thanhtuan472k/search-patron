version: '3.8'

services:
  app:
    build: .
    ports:
      - '5173:5173'
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
    # Optional: Restart the service if it crashes
    restart: unless-stopped